<h1> <%= @post.title %> </h1> 
<small><%= @post.created_at.strftime("%b %d. %Y") %></small>
<p><%= @post.body %> </p>
<%= link_to "Edit Post", edit_post_path %> | <%= link_to "Delete", posts_path, method: :delete, posts: { confirm: "Are you sure?"} %> |
<%= link_to "Go back",posts_path %> 

#not deleting data